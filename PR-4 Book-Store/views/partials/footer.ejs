</main>

<footer class="simple-footer">
    <div class="container">
        <div class="footer-content">
            <div class="copyright">
                © <%= new Date().getFullYear() %> Book Store — Clean UI • Fast Performance • Beautiful Design
            </div>
            <div class="footer-links">
                <a href="/" class="footer-link">Home</a>
                <span class="link-separator">•</span>
                <a href="/" class="footer-link">Features</a>
                <span class="link-separator">•</span>
                <a href="/" class="footer-link">About</a>
                <span class="link-separator">•</span>
                <a href="/" class="footer-link">Contact</a>
            </div>
        </div>
    </div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<script>
    (function () {
        const input = document.getElementById('bookSearch');
        if (!input) return;
        input.addEventListener('input', function () {
            const q = this.value.trim().toLowerCase();
            const rows = document.querySelectorAll('table.table tbody tr');
            rows.forEach(row => {
                const title = (row.cells[1] && row.cells[1].textContent || '').toLowerCase();
                const author = (row.cells[2] && row.cells[2].textContent || '').toLowerCase();
                row.style.display = (title.includes(q) || author.includes(q)) ? '' : 'none';
            });
        });
    })();
</script>

<style>
    /* Simple Footer Styles */
    .simple-footer {
        background: linear-gradient(135deg, rgba(42, 67, 161, 0.95), rgba(74, 111, 255, 0.9));
        color: white;
        padding: 30px 0;
        border-top: 4px solid var(--accent);
        position: relative;
        overflow: hidden;
    }
    
    .simple-footer::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: 
            radial-gradient(circle at 20% 80%, rgba(255, 209, 102, 0.1) 0%, transparent 50%),
            radial-gradient(circle at 80% 20%, rgba(255, 107, 139, 0.1) 0%, transparent 50%);
        z-index: 1;
    }
    
    .footer-content {
        position: relative;
        z-index: 2;
        display: flex;
        justify-content: space-between;
        align-items: center;
        flex-wrap: wrap;
        gap: 20px;
    }
    
    .copyright {
        font-size: 15px;
        font-weight: 500;
        color: rgba(255, 255, 255, 0.95);
    }
    
    .copyright .highlight {
        color: #ffd166;
        font-weight: 600;
    }
    
    .footer-links {
        display: flex;
        align-items: center;
        gap: 15px;
        flex-wrap: wrap;
    }
    
    .footer-link {
        color: rgba(255, 255, 255, 0.9);
        text-decoration: none;
        font-weight: 500;
        font-size: 15px;
        transition: all 0.3s ease;
        padding: 5px 10px;
        border-radius: 6px;
        position: relative;
    }
    
    .footer-link:hover {
        color: #ffd166;
        background: rgba(255, 255, 255, 0.1);
        transform: translateY(-2px);
    }
    
    .footer-link::after {
        content: '';
        position: absolute;
        bottom: -2px;
        left: 50%;
        width: 0;
        height: 2px;
        background: #ffd166;
        transition: all 0.3s ease;
        transform: translateX(-50%);
    }
    
    .footer-link:hover::after {
        width: 80%;
    }
    
    .link-separator {
        color: rgba(255, 255, 255, 0.5);
        font-size: 14px;
        font-weight: 300;
    }
    
    /* Responsive Design */
    @media (max-width: 768px) {
        .footer-content {
            flex-direction: column;
            text-align: center;
            gap: 15px;
        }
        
        .footer-links {
            justify-content: center;
        }
    }
    
    @media (max-width: 480px) {
        .copyright {
            font-size: 14px;
            line-height: 1.5;
        }
        
        .footer-links {
            gap: 10px;
        }
        
        .footer-link {
            font-size: 14px;
            padding: 4px 8px;
        }
    }
</style>
</body>
</html>