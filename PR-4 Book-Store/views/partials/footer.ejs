</main>

<footer class="small-note">
    <div class="container">© <%= new Date().getFullYear() %> Book Store — Clean UI demo</div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<script>
    (function () {
        const input = document.getElementById('bookSearch');
        if (!input) return;
        input.addEventListener('input', function () {
            const q = this.value.trim().toLowerCase();
            const rows = document.querySelectorAll('table.table tbody tr');
            rows.forEach(row => {
                const title = (row.cells[1] && row.cells[1].textContent || '').toLowerCase();
                const author = (row.cells[2] && row.cells[2].textContent || '').toLowerCase();
                row.style.display = (title.includes(q) || author.includes(q)) ? '' : 'none';
            });
        });
    })();
</script>
</body>

</html>