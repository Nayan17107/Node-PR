<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Book Store</title>

    <!-- Bootstrap (CDN) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        /* Internal CSS (used as requested) */
        :root {
            --primary: #0d6efd;
            --accent: #ff6b6b;
            --muted: #6c757d;
            --bg: linear-gradient(180deg, #f6fbff 0%, #f0f6fb 100%);
            --card: #ffffff;
        }

        body {
            background: var(--bg);
            font-family: "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            color: #222;
        }

        .brand {
            display: flex;
            align-items: center;
            gap: 10px;
            font-weight: 700;
            color: var(--primary);
        }

        .brand svg {
            filter: drop-shadow(0 4px 10px rgba(13, 110, 253, 0.08));
        }

        .card-custom {
            background: var(--card);
            border-radius: 12px;
            padding: 18px;
            box-shadow: 0 8px 30px rgba(13, 110, 253, 0.06);
            margin-bottom: 20px;
            transition: transform .18s ease, box-shadow .18s ease;
        }

        .card-custom:hover {
            transform: translateY(-6px);
            box-shadow: 0 18px 48px rgba(13, 110, 253, 0.09);
        }

        .table tbody tr:hover {
            background: rgba(13, 110, 253, 0.03);
            transition: background .12s ease;
        }

        .btn,
        .btn-accent,
        .btn-outline-primary {
            transition: transform .12s ease, box-shadow .12s ease;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 24px rgba(13, 110, 253, 0.06);
        }

        .btn-accent {
            background: var(--accent);
            border-color: var(--accent);
            color: #fff;
        }

        .table thead th {
            background: linear-gradient(90deg, rgba(13, 110, 253, 0.12), rgba(255, 107, 107, 0.06));
        }

        .search-input {
            max-width: 320px;
        }

        .badge-count {
            background: linear-gradient(90deg, #ff6b6b, #0d6efd);
            color: #fff;
            padding: .35rem .6rem;
            border-radius: 999px;
            font-weight: 700;
        }

        .form-label {
            font-weight: 600;
            color: #333;
        }

        footer.small-note {
            padding: 16px 0;
            text-align: center;
            color: var(--muted);
        }

        @media (max-width:767px) {
            .table-responsive {
                overflow: auto;
            }
        }
    </style>

</head>

<body>

    <nav class="navbar navbar-expand-lg bg-white shadow-sm">
        <div class="container">
            <a class="navbar-brand brand" href="/">
                <svg width="32" height="32" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <path d="M4 19V5a1 1 0 011-1h10l4 4v11a1 1 0 01-1 1H5a1 1 0 01-1-1z" fill="#0d6efd" />
                </svg>
                <span>Book <strong style="color:var(--accent)">Store</strong></span>
            </a>

            <div class="ms-auto d-none d-lg-block">
                <a href="/" class="btn btn-outline-primary">Home</a>
            </div>
        </div>
    </nav>

    <main class="container py-4">