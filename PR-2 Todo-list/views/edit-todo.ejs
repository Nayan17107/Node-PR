<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Todo</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&family=Space+Grotesk:wght@500;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-1: #edf4ff;
            --bg-2: #f8f2ff;
            --panel: rgba(255, 255, 255, 0.84);
            --line: #e2e8f0;
            --text: #0f172a;
            --muted: #64748b;
            --primary: #2563eb;
            --primary-strong: #1d4ed8;
            --shadow: 0 20px 45px rgba(15, 23, 42, 0.12);
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            min-height: 100vh;
            color: var(--text);
            font-family: "Poppins", sans-serif;
            background:
                radial-gradient(circle at 10% 10%, #dbeafe 0%, transparent 33%),
                radial-gradient(circle at 90% 0%, #f5d0fe 0%, transparent 34%),
                linear-gradient(140deg, var(--bg-1), var(--bg-2));
        }

        .shell {
            width: min(780px, 100% - 2rem);
            margin: 38px auto;
        }

        .card {
            border-radius: 22px;
            overflow: hidden;
            background: var(--panel);
            border: 1px solid rgba(255, 255, 255, 0.85);
            box-shadow: var(--shadow);
            backdrop-filter: blur(8px);
        }

        .card-head {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 12px;
            padding: 18px 20px;
            border-bottom: 1px solid var(--line);
            background: linear-gradient(180deg, rgba(255, 255, 255, 0.72), rgba(255, 255, 255, 0.2));
        }

        .card-head h1 {
            margin: 0;
            font-family: "Space Grotesk", sans-serif;
            font-size: clamp(1.4rem, 2.1vw, 2rem);
            letter-spacing: -0.02em;
        }

        .sub {
            margin: 6px 0 0;
            color: var(--muted);
            font-size: 0.88rem;
        }

        .card-body {
            padding: 20px;
        }

        .grid {
            display: grid;
            gap: 14px;
        }

        .field label {
            display: block;
            font-size: 0.83rem;
            color: var(--muted);
            margin-bottom: 6px;
            font-weight: 500;
        }

        .control {
            width: 100%;
            border: 1px solid var(--line);
            border-radius: 12px;
            padding: 11px 12px;
            font-family: inherit;
            background: #fff;
            outline: 0;
            transition: border-color 0.18s ease, box-shadow 0.18s ease;
        }

        .control:focus {
            border-color: #93c5fd;
            box-shadow: 0 0 0 4px rgba(147, 197, 253, 0.3);
        }

        .control[readonly] {
            background: #f8fafc;
            color: #475569;
        }

        textarea.control {
            min-height: 110px;
            resize: vertical;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            border: 0;
            border-radius: 12px;
            padding: 10px 16px;
            text-decoration: none;
            font-family: inherit;
            font-size: 0.9rem;
            font-weight: 600;
            cursor: pointer;
            transition: transform 0.18s ease, box-shadow 0.2s ease, background-color 0.2s ease, color 0.2s ease;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary), #3b82f6);
            color: #fff;
            box-shadow: 0 12px 25px rgba(37, 99, 235, 0.28);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 16px 30px rgba(37, 99, 235, 0.34);
            background: linear-gradient(135deg, var(--primary-strong), var(--primary));
        }

        .btn-ghost {
            background: #fff;
            color: var(--primary);
            border: 1px solid #bfdbfe;
        }

        .btn-ghost:hover {
            transform: translateY(-1px);
            background: #eff6ff;
            border-color: #93c5fd;
        }

        .actions {
            margin-top: 18px;
            display: flex;
            gap: 10px;
            justify-content: flex-end;
            flex-wrap: wrap;
        }

        .foot {
            margin-top: 12px;
            text-align: center;
            color: var(--muted);
            font-size: 0.82rem;
        }

        @media (max-width: 640px) {
            .card-head {
                flex-direction: column;
                align-items: flex-start;
            }
        }
    </style>
</head>

<body>
    <div class="shell">
        <section class="card">
            <header class="card-head">
                <div>
                    <h1>Edit Todo</h1>
                    <p class="sub">Update the task details and save changes.</p>
                </div>
                <a class="btn btn-ghost" href="/">Back to List</a>
            </header>

            <div class="card-body">
                <form action="/update-todo/<%= todo.id %>" method="post">
                    <div class="grid">
                        <div class="field">
                            <label for="id">Task ID</label>
                            <input id="id" class="control" type="text" name="id" value="<%= todo.id %>" readonly>
                        </div>

                        <div class="field">
                            <label for="title">Title</label>
                            <input id="title" class="control" type="text" name="title" value="<%= todo.title %>" required>
                        </div>

                        <div class="field">
                            <label for="description">Description</label>
                            <textarea id="description" class="control" name="description"><%= todo.description %></textarea>
                        </div>

                        <div class="field">
                            <label for="time">Time</label>
                            <input id="time" class="control" type="time" name="time" value="<%= todo.time %>">
                        </div>
                    </div>

                    <div class="actions">
                        <a class="btn btn-ghost" href="/">Cancel</a>
                        <button class="btn btn-primary" type="submit">Update Todo</button>
                    </div>
                </form>
            </div>
        </section>

        <footer class="foot">Changes are stored in memory for this server session.</footer>
    </div>
</body>

</html>
