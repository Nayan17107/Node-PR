<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo Flow</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&family=Space+Grotesk:wght@500;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-1: #f3f7ff;
            --bg-2: #f5f1ff;
            --panel: rgba(255, 255, 255, 0.8);
            --line: #e2e8f0;
            --text: #0f172a;
            --muted: #64748b;
            --primary: #2563eb;
            --primary-strong: #1d4ed8;
            --danger: #dc2626;
            --shadow: 0 20px 45px rgba(15, 23, 42, 0.12);
            --shadow-soft: 0 12px 30px rgba(15, 23, 42, 0.08);
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            min-height: 100vh;
            font-family: "Poppins", sans-serif;
            color: var(--text);
            background:
                radial-gradient(circle at 12% 18%, #dbeafe 0%, transparent 32%),
                radial-gradient(circle at 88% 5%, #e9d5ff 0%, transparent 36%),
                linear-gradient(145deg, var(--bg-1), var(--bg-2));
        }

        .page {
            width: min(1120px, 100% - 2rem);
            margin: 34px auto;
        }

        .topbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 12px;
            margin-bottom: 18px;
        }

        .title-wrap h1 {
            margin: 0;
            font-family: "Space Grotesk", sans-serif;
            font-size: clamp(1.6rem, 2.2vw, 2.4rem);
            letter-spacing: -0.02em;
        }

        .title-wrap p {
            margin: 6px 0 0;
            color: var(--muted);
            font-size: 0.92rem;
        }

        .btn {
            border: 0;
            cursor: pointer;
            border-radius: 12px;
            padding: 10px 16px;
            font-weight: 600;
            font-family: inherit;
            font-size: 0.9rem;
            transition: transform 0.18s ease, box-shadow 0.2s ease, background-color 0.2s ease, color 0.2s ease;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary), #3b82f6);
            color: #fff;
            box-shadow: 0 12px 25px rgba(37, 99, 235, 0.28);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 16px 28px rgba(37, 99, 235, 0.35);
            background: linear-gradient(135deg, var(--primary-strong), var(--primary));
        }

        .panel-grid {
            display: grid;
            gap: 18px;
            grid-template-columns: 360px 1fr;
        }

        .panel {
            background: var(--panel);
            backdrop-filter: blur(8px);
            border: 1px solid rgba(255, 255, 255, 0.9);
            border-radius: 20px;
            box-shadow: var(--shadow);
            overflow: hidden;
        }

        .panel-head {
            padding: 18px 20px 12px;
            border-bottom: 1px solid var(--line);
            background: linear-gradient(180deg, rgba(255, 255, 255, 0.72), rgba(255, 255, 255, 0.2));
        }

        .panel-head h2 {
            margin: 0;
            font-size: 1.05rem;
            font-family: "Space Grotesk", sans-serif;
        }

        .panel-body {
            padding: 16px 20px 20px;
        }

        .field {
            margin-bottom: 14px;
        }

        .field label {
            display: block;
            font-size: 0.82rem;
            color: var(--muted);
            margin-bottom: 6px;
            font-weight: 500;
        }

        .control {
            width: 100%;
            border: 1px solid var(--line);
            border-radius: 12px;
            padding: 11px 12px;
            background: #fff;
            outline: 0;
            font-family: inherit;
            transition: border-color 0.18s ease, box-shadow 0.18s ease;
        }

        .control:focus {
            border-color: #93c5fd;
            box-shadow: 0 0 0 4px rgba(147, 197, 253, 0.3);
        }

        textarea.control {
            min-height: 88px;
            resize: vertical;
        }

        .form-actions {
            margin-top: 16px;
            display: flex;
            justify-content: flex-end;
        }

        .todo-list {
            display: grid;
            gap: 12px;
        }

        .todo-card {
            border: 1px solid #dbeafe;
            background: #fff;
            border-radius: 16px;
            padding: 14px;
            transition: transform 0.2s ease, box-shadow 0.2s ease, border-color 0.2s ease;
            box-shadow: var(--shadow-soft);
        }

        .todo-card:hover {
            transform: translateY(-3px);
            box-shadow: 0 18px 34px rgba(30, 64, 175, 0.17);
            border-color: #93c5fd;
        }

        .todo-top {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            gap: 10px;
        }

        .todo-id {
            font-size: 0.77rem;
            font-weight: 600;
            color: #1e3a8a;
            background: #dbeafe;
            padding: 3px 9px;
            border-radius: 999px;
        }

        .todo-time {
            color: var(--muted);
            font-size: 0.8rem;
        }

        .todo-title {
            margin: 10px 0 6px;
            font-size: 1rem;
            font-weight: 600;
        }

        .todo-desc {
            margin: 0;
            color: #334155;
            font-size: 0.9rem;
            line-height: 1.45;
        }

        .todo-actions {
            margin-top: 12px;
            display: flex;
            gap: 8px;
        }

        .action-link {
            text-decoration: none;
            font-size: 0.84rem;
            font-weight: 600;
            padding: 7px 11px;
            border-radius: 10px;
            transition: transform 0.18s ease, background-color 0.18s ease, color 0.18s ease;
        }

        .action-edit {
            background: #eff6ff;
            color: #1d4ed8;
        }

        .action-edit:hover {
            transform: translateY(-1px);
            background: #dbeafe;
        }

        .action-delete {
            background: #fef2f2;
            color: var(--danger);
        }

        .action-delete:hover {
            transform: translateY(-1px);
            background: #fee2e2;
        }

        .empty {
            text-align: center;
            color: var(--muted);
            padding: 26px 12px;
            border: 1px dashed #cbd5e1;
            border-radius: 14px;
            background: #f8fafc;
        }

        .page-footer {
            margin-top: 16px;
            text-align: center;
            color: var(--muted);
            font-size: 0.82rem;
        }

        @media (max-width: 960px) {
            .panel-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>

<body>
    <div class="page">
        <header class="topbar">
            <div class="title-wrap">
                <h1>Todo Flow</h1>
                <p>Plan faster and keep every task in one place.</p>
            </div>
            <button class="btn btn-primary" onclick="location.reload();" type="button">Refresh</button>
        </header>

        <main class="panel-grid">
            <section class="panel">
                <div class="panel-head">
                    <h2>Create New Task</h2>
                </div>
                <div class="panel-body">
                    <form action="/add-todo" method="post">
                        <div class="field">
                            <label for="id">Task ID</label>
                            <input id="id" class="control" type="text" name="id" required>
                        </div>

                        <div class="field">
                            <label for="title">Title</label>
                            <input id="title" class="control" type="text" name="title" required>
                        </div>

                        <div class="field">
                            <label for="description">Description</label>
                            <textarea id="description" class="control" name="description" placeholder="Add a short note..."></textarea>
                        </div>

                        <div class="field">
                            <label for="time">Time</label>
                            <input id="time" class="control" type="time" name="time">
                        </div>

                        <div class="form-actions">
                            <button class="btn btn-primary" type="submit">Add Todo</button>
                        </div>
                    </form>
                </div>
            </section>

            <section class="panel">
                <div class="panel-head">
                    <h2>All Tasks</h2>
                </div>
                <div class="panel-body">
                    <% if (!todos || todos.length===0) { %>
                        <div class="empty">No tasks yet. Add your first todo from the left panel.</div>
                        <% } else { %>
                            <div class="todo-list">
                                <% todos.forEach((t)=> { %>
                                    <article class="todo-card">
                                        <div class="todo-top">
                                            <span class="todo-id">ID: <%= t.id %></span>
                                            <span class="todo-time">
                                                <%= t.time || "No time set" %>
                                            </span>
                                        </div>
                                        <h3 class="todo-title">
                                            <%= t.title %>
                                        </h3>
                                        <p class="todo-desc">
                                            <%= t.description || "No description provided." %>
                                        </p>
                                        <div class="todo-actions">
                                            <a class="action-link action-edit" href="/edit-todo/<%= t.id %>">Edit</a>
                                            <a class="action-link action-delete" href="/delete-todo/<%= t.id %>">Delete</a>
                                        </div>
                                    </article>
                                    <% }) %>
                            </div>
                            <% } %>
                </div>
            </section>
        </main>

        <footer class="page-footer">Simple in-memory Todo app</footer>
    </div>
</body>

</html>
